application:
  locale: ru_RU

spring:
  shell:
    interactive:
      enabled: false

test:
  file-name: test-tasks.csv
  passing-score: 40.0

bean:
  output: "#{T(java.lang.System).out}"
  input: "#{ T(java.lang.System).in}"